# TAVE MT 발표 자료 수정 가이드

이 문서 하나만 보면 코드를 몰라도 내용 추가/순서 변경/디자인(색·크기·위치·폰트) 수정을 할 수 있도록 정리했습니다.

---

## 0. 미리보기 하는 법

`index.html` 파일을 더블클릭하면 기본 브라우저에서 바로 열립니다. (서버 필요 없음)

수정 후에는 그냥 브라우저에서 **새로고침(Cmd+R)** 하면 바로 반영됩니다.

- 다음/이전 슬라이드: 화면 양옆 동그란 버튼 클릭 또는 키보드 방향키(← →)

---

## 1. 파일이 어떤 역할을 하는지

```
TAVE_MT/
├── index.html              ← 그냥 틀(뼈대). 거의 건드릴 일 없음
├── GUIDE.md                 ← 지금 보고 있는 이 문서
├── image/
│   ├── background.png       ← 모든 슬라이드 공통 배경
│   └── 인물퀴즈/              ← 인물 퀴즈용 사진들
├── js/
│   ├── slides-data.js       ★★★ 내용/순서 수정은 여기서만 하면 됨 ★★★
│   ├── templates.js         ← 슬라이드 "종류"별 구조(디자인 골격). 거의 건드릴 일 없음
│   └── main.js               ← 버튼/키보드 동작 로직. 건드릴 일 없음
└── css/
    ├── base.css              ← 공통 부품(버튼, 글자 스타일, 패널, 뱃지, 원형 정답 등)
    └── slides/
        ├── title.css          ← 표지 슬라이드 전용 디자인
        ├── rules.css           ← "규칙 안내" 슬라이드 전용 디자인
        └── quiz.css            ← "인물 퀴즈 사진/정답" 슬라이드 전용 디자인
```

**내용(글자, 순서, 사진, 정답)을 바꾸고 싶다 → `js/slides-data.js`만 보면 됩니다.**
**디자인(색, 크기, 위치, 폰트 굵기)을 바꾸고 싶다 → `css/` 폴더 안의 파일을 보면 됩니다.**

---

## 2. 슬라이드 순서/내용 바꾸기 — `js/slides-data.js`

이 파일은 `SLIDES`라는 목록(배열) 하나로 이루어져 있고, **위에서부터 적힌 순서 그대로** 발표에 나옵니다.

```js
const SLIDES = [
  { type: 'title', ... },     // 1번째로 보여짐
  { type: 'rules', ... },     // 2번째로 보여짐
  { type: 'quiz-photo', ... },// 3번째로 보여짐
  ...
];
```

### 2-1. 순서를 바꾸고 싶을 때

`{ ... }` 덩어리 하나가 슬라이드 한 장입니다. 이 덩어리를 통째로 잘라서(Cmd+X) 원하는 위치에 붙여넣으면(Cmd+V) 순서가 바뀝니다. **덩어리 끝에 붙은 쉼표(`,`)까지 같이 옮겨야** 오류가 안 납니다.

### 2-2. 슬라이드를 삭제하고 싶을 때

그 슬라이드의 `{ ... }` 덩어리 전체를 지우면 됩니다 (앞뒤 쉼표는 하나만 남도록 정리).

### 2-3. 현재 만들어진 슬라이드 종류(type) 3가지

| type | 용도 | 예시 |
|---|---|---|
| `title` | 표지 슬라이드 | "어서오세요 18기에 / TAVE MT" |
| `rules` | 게임 규칙 안내 (게임마다 재사용) | "인물 퀴즈" 규칙 안내 |
| `quiz-photo` / `quiz-answer` | 인물 퀴즈 사진 → 정답 세트 (인물마다 재사용) | 01번 아이유 사진/정답 |

---

## 3. 새 게임 "규칙 안내" 페이지 추가하기

`slides-data.js` 안에 있는 `rules` 타입 객체를 복사해서 아래처럼 붙여넣고 내용만 바꾸면 됩니다.

```js
{
  type: 'rules',
  gameName: '초성 퀴즈',        // 흰색 말풍선에 들어갈 게임 이름
  badgeColor: '#7a1616',        // 게임 이름 글자 색 (16진수 색상 코드)
  rulesTitle: '규칙 안내',       // 박스 제목 (보통 안 바꿔도 됨)
  rules: [
    '초성이 제시되면 정답을 맞히는 게임입니다.',   // ① 로 자동 표시됨
    '가장 먼저 외친 팀이 정답을 말할 기회를 얻습니다.', // ② 로 자동 표시됨
    '오답 시 다음 팀에게 기회가 넘어갑니다.',       // ③ 로 자동 표시됨
  ],
  example: {
    label: '예시',
    items: ['ㅇ', 'ㅈ'],          // 동그라미 안에 들어갈 글자들, 개수 자유
  },
},
```

- `rules` 배열의 줄 수만큼 ①②③... 번호가 자동으로 붙습니다. 항목을 늘리거나 줄여도 알아서 처리됩니다.
- 예시가 필요 없는 게임이면 `example: { ... }` 부분을 통째로 지우면 아래 "예시" 박스 자체가 안 나옵니다.
- `badgeColor`를 안 적으면 기본값(남색 `#16247d`)으로 나옵니다.

---

## 4. 인물 퀴즈 "사진 + 정답" 세트 추가하기

인물 퀴즈는 **사진 페이지, 정답 페이지가 한 세트**로 묶여서 재사용되도록 만들어 놨습니다. `slides-data.js` 맨 위에 있는 이 한 줄만 복사하면 됩니다.

```js
...personQuizSet(2, 'image/인물퀴즈/02_스윙스.jpeg', '스윙스'),
```

- 첫 번째 값 `2` → 왼쪽 위에 표시되는 번호(02.)
- 두 번째 값 → 사진 파일 경로 (`image/인물퀴즈/` 폴더에 사진 넣고 파일명 적기)
- 세 번째 값 → 정답 페이지에 나올 텍스트

이 줄 하나가 **사진 슬라이드 1장 + 정답 슬라이드 1장, 총 2장**으로 자동으로 펼쳐집니다. `slides-data.js` 안에 이미 예시로 2번, 3번이 주석(`//`) 처리되어 있으니, 맨 앞의 `//`만 지우면 바로 활성화됩니다.

> 사진은 `image/인물퀴즈/` 폴더 안에 이미 들어있는 파일들을 그대로 쓰면 되고, 새 사진을 추가하려면 그 폴더에 파일을 넣고 경로만 맞게 적으면 됩니다. 파일명에 띄어쓰기가 있으면 그대로 써도 되지만, 가능하면 `01_아이유.jpg`처럼 언더바(`_`)로 붙이는 걸 추천합니다.

---

## 5. 완전히 새로운 슬라이드 "종류"가 필요할 때

기존 3가지 틀(`title`, `rules`, `quiz-photo`/`quiz-answer`)로 표현이 안 되는 새로운 형태의 페이지(예: 팀 소개, 순위표 등)가 필요하면, 저(Claude)에게 참고 이미지나 원하는 레이아웃을 알려주면 같은 방식으로 재사용 가능한 새 템플릿을 만들어 드립니다. 이 부분은 `js/templates.js`와 `css/slides/`에 새 파일을 추가하는 방식이라 직접 만들기보다는 요청해서 받는 걸 추천합니다.

---

## 6. 디자인(색깔 / 크기 / 굵기 / 위치) 수정하기

모든 크기와 위치는 `vh`(뷰포트 높이의 %) 단위를 씁니다. 슬라이드 세로 길이를 100이라고 봤을 때 몇 %인지로 이해하면 됩니다. 숫자를 키우면 커지고/내려가고, 줄이면 작아지고/올라갑니다.

### 6-1. 폰트(평창 평화체) 관련

폰트 자체는 `css/base.css` 맨 위 `@font-face` 부분에서 CDN으로 불러옵니다. 특별한 이유가 없다면 건드릴 필요 없습니다.

### 6-2. 표지 슬라이드 ("TAVE MT" 큰 글씨) — `css/slides/title.css`

```css
.s1-main {
  top: 53%;          /* 세로 위치 (%가 클수록 아래로) */
  font-size: 16.5vh;  /* 글자 크기 */
  -webkit-text-stroke: 0.07em #f4c94a; /* 테두리 두께(=볼드해 보이는 정도) & 색 */
}
```
- **글자를 더 키우고 싶다**: `font-size` 숫자를 올린다 (예: `16.5vh` → `18vh`)
- **더 아래로 내리고 싶다**: `top` 숫자를 올린다 (예: `53%` → `56%`)
- **더 두껍게(볼드하게) 보이고 싶다**: `-webkit-text-stroke`의 `0.07em` 숫자를 올린다

부제목("어서오세요 18기에")은 바로 위 `.s1-title` 블록에서 같은 방식으로 조절합니다.

그라데이션 색(초록→흰색→파랑)은 `css/base.css`의 `.grad-outline` 안 `background: linear-gradient(...)` 줄에서 색상 코드를 바꾸면 됩니다. 이 클래스는 표지 제목과 좌상단 로고가 함께 쓰기 때문에, 여기를 바꾸면 둘 다 같이 바뀝니다.

### 6-3. 좌상단 "TAVE MT" 로고 & 게임 이름 뱃지 — `css/slides/rules.css`

```css
.rules-header {
  top: 6%;      /* 헤더 전체 세로 위치 */
  left: 4.5%;   /* 헤더 전체 가로 위치 */
  gap: 2.4vh;   /* 로고와 뱃지 사이 간격 */
}
.rules-header .mini-logo {
  font-size: 4.6vh;  /* 로고 글자 크기 */
}
```

흰색 말풍선(게임 이름) 자체의 글자 크기/여백은 `css/base.css`의 `.badge-bubble`에서 조절합니다.
```css
.badge-bubble {
  font-size: 2.4vh;    /* 게임 이름 글자 크기 */
  padding: 0.9vh 2vh;  /* 말풍선 내부 여백(=박스 크기) */
}
```
게임 이름 글자 **색**은 CSS가 아니라 `slides-data.js`의 `badgeColor` 값으로 슬라이드마다 다르게 지정합니다 (3, 4번 항목 참고).

### 6-4. 규칙 안내 박스 — `css/slides/rules.css`

```css
.rules-panel {
  top: 19%; left: 4.5%; right: 4.5%;  /* 박스의 위치/너비 */
  padding: 2.6vh 3.2vh;                /* 박스 안쪽 여백 */
}
.rules-title {
  font-size: 2.7vh;  /* "규칙 안내" 글자 크기 */
}
.rules-list li {
  font-size: 2vh;    /* 규칙 문장 글자 크기 */
  line-height: 1.5;  /* 줄 간격 */
  color: #f2f2f2;    /* 글자 색 */
}
.rules-list .num {
  color: #ffd977;    /* ①②③ 번호 색 */
}
```

박스 테두리 색/두께나 안쪽 배경색(반투명 남색)은 `css/base.css`의 `.panel`에서 공통으로 바꿀 수 있습니다.
```css
.panel {
  border: 2px solid #e0b45c;          /* 테두리 두께/색 */
  background: rgba(6, 10, 26, 0.62);  /* 안쪽 배경색 (마지막 0.62가 투명도, 0~1) */
}
```
> 이 `.panel` 클래스는 규칙 박스, 예시 태그, 인물 퀴즈 박스가 전부 같이 쓰기 때문에, 여기를 고치면 전체 박스 스타일이 한 번에 바뀝니다. 특정 슬라이드 종류만 바꾸고 싶다면 `rules.css`/`quiz.css`처럼 해당 파일 안의 클래스(`.rules-panel`, `.quiz-panel` 등)를 따로 수정하세요.

### 6-5. 예시 + 정답 동그라미 — `css/slides/rules.css`, `css/base.css`

```css
/* rules.css */
.example-row { bottom: 6.5%; left: 4.5%; right: 4.5%; }
.example-tag { font-size: 2.3vh; padding: 1.6vh 2.6vh; }

/* base.css */
.answer-circle {
  width: 9vh; height: 9vh;   /* 동그라미 크기 (가로세로 같이 바꿀 것) */
  font-size: 4.2vh;          /* 동그라미 안 글자 크기 */
  background: radial-gradient(circle at 35% 30%, #ffe9a8, #d9a53f); /* 동그라미 색 */
}
```

### 6-6. 인물 퀴즈 사진/정답 페이지 — `css/slides/quiz.css`

```css
.quiz-panel { top: 19%; left: 4.5%; right: 4.5%; bottom: 5%; } /* 박스 크기/위치 */

.quiz-number { font-size: 2.6vh; }  /* "01." 번호 글자 크기 */

.quiz-photo {
  max-height: 46vh;   /* 사진 최대 세로 크기 (사진 비율은 자동 유지됨) */
  max-width: 46vw;
  border: 0.6vh solid #ffffff;  /* 사진 흰 테두리 두께 */
}

.quiz-answer-text {
  font-size: 9vh;      /* 정답 글자 크기 */
  color: #dcb354;      /* 정답 글자 색 */
}
```

---

## 7. 좌우 이동 버튼 / 카운터 — `css/base.css`

```css
.nav-btn {
  width: 3.2vh; height: 3.2vh;               /* 버튼 크기 */
  border: 2px solid rgba(255, 214, 102, 0.75); /* 테두리 색/두께 */
}
#prevBtn { left: 1.2vw; }   /* 왼쪽 버튼 위치 */
#nextBtn { right: 1.2vw; }  /* 오른쪽 버튼 위치 */
```

버튼은 첫 슬라이드에서는 "이전"이, 마지막 슬라이드에서는 "다음"이 자동으로 흐리게(비활성화) 처리됩니다. 이건 `js/main.js`가 슬라이드 개수를 보고 자동으로 계산하는 부분이라 따로 신경 쓸 필요 없습니다.

---

## 8. 자주 하는 실수

- **쉼표(,) 빠뜨림**: `slides-data.js`에서 객체를 복사/삭제할 때 앞뒤 쉼표 개수를 꼭 확인하세요. 쉼표가 어긋나면 화면이 아예 안 뜰 수 있습니다.
- **따옴표 안 닫음**: 문장 앞뒤의 `'...'` 또는 `"..."`를 짝 맞춰야 합니다.
- **색상 코드**: `#16247d`처럼 `#` + 6자리 16진수 형식을 지켜야 합니다.
- **저장 후 새로고침**: 파일 저장을 안 하면 브라우저에 반영되지 않습니다.
- 뭔가 깨졌다면, 브라우저에서 우클릭 → "검사"(개발자 도구) → **Console** 탭에 빨간 에러 메시지가 있는지 확인하면 어디가 잘못됐는지 힌트를 얻을 수 있습니다.

---

## 9. 요약: "이럴 땐 어디를 고치면 되나요?"

| 하고 싶은 것 | 고칠 파일 |
|---|---|
| 슬라이드 순서 변경 | `js/slides-data.js` |
| 슬라이드 추가/삭제 | `js/slides-data.js` |
| 게임 규칙 문구 수정 | `js/slides-data.js` |
| 인물 퀴즈 사진/정답 추가 | `js/slides-data.js` |
| 글자 크기/두께/위치 | `css/base.css` 또는 `css/slides/*.css` |
| 색상(테두리, 배경, 글자) | `css/base.css` 또는 `css/slides/*.css` |
| 새로운 형태의 페이지 디자인 | Claude에게 요청 (`js/templates.js` + `css/slides/` 새 파일 필요) |
